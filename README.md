# Web scrapping using Selenium
a Python-based Telegram bot that automates login and data extraction from the university portal using Selenium WebDriver, parses grade data, and sends scheduled notifications via the Telegram Bot API.
